// // properties/+page.ts
// import { db } from '../api/firebase/+server';
// import { collection, getDocs } from 'firebase/firestore';
// import { json } from '@sveltejs/kit'


// // Use the db variable to interact with the Firestore database
// const propertiesCollection = collection(db, 'properties');
// const propiedades = await getDocs(propertiesCollection);

// console.log(await json(propiedades), "propiedades desde page.ts");

// ... rest of the code ...
